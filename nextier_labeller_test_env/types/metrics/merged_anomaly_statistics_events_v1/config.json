{
  "key": "merged_anomaly_statistics_events_v1",
  "displayName": "Merged Anomaly Event Drilldown",
  "description": "Active-run contiguous anomaly windows for the selected scope. Sapphire-gated (v3 stores, sapphire stage ordinal).",
  "sql": "SELECT * FROM (\nWITH active_manifest AS (\n  SELECT *\n  FROM featurestore:merged_anomaly_processing_manifest_v3\n  WHERE status = 'active'\n    AND algorithm_version = 'sapphire-gate-v1'\n    AND fleet_name = :fleet_name\n    AND pad_name = :pad_name\n    AND (\n      COALESCE(CAST(:well_name AS TEXT), '') IN ('', '__ALL__')\n      OR name = CAST(:well_name AS TEXT)\n    )\n)\nSELECT\n  e.event_id,\n  e.name AS well_name,\n  e.stage_num AS stage_num,\n  e.anomaly,\n  e.anomaly_display,\n  e.start_ts,\n  e.end_ts,\n  e.duration_sec,\n  e.duration_min\nFROM active_manifest m\nJOIN featurestore:merged_anomaly_events_v3 e\n  ON e.processing_run_id = m.active_run_id\n AND e.name = m.name\n AND e.stage_num = m.stage_num\n AND e.algorithm_version = m.algorithm_version\nORDER BY e.start_ts DESC, e.name, e.stage_num\n) q",
  "valueSchema": {
    "type": "object",
    "properties": {
      "event_id": {
        "type": "string"
      },
      "well_name": {
        "type": "string"
      },
      "stage_num": {
        "type": "number"
      },
      "anomaly": {
        "type": "string"
      },
      "anomaly_display": {
        "type": "string"
      },
      "start_ts": {
        "type": "string",
        "format": "date-time"
      },
      "end_ts": {
        "type": "string",
        "format": "date-time"
      },
      "duration_sec": {
        "type": "number"
      },
      "duration_min": {
        "type": "number"
      }
    },
    "required": []
  },
  "paramsSchema": {
    "type": "object",
    "properties": {
      "fleet_name": {
        "type": "string"
      },
      "pad_name": {
        "type": "string"
      },
      "well_name": {
        "type": "string"
      }
    },
    "required": [
      "fleet_name",
      "pad_name",
      "well_name"
    ]
  }
}
